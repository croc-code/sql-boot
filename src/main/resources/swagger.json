{
  "swagger": "2.0",
  "info": {
    "title": "Commons sql-boot configuration",
    "version": "v1"
  },
  "schemes": [
    "http", "https"
  ],
  "paths": {
    "/api/table/{schema_name}": {
      "get": {
        "tags": ["Table"],
        "parameters": [
          {"name": "schema_name", "in": "path", "description": "Schema name",
            "required": true, "type": "string"}],
        "responses":  {"200" : {"description": "OK"}},
        "produces": ["application/json"]
      }
    },
    "/api/table/{schema_name}.{table_name}": {
      "get": {
        "tags": ["Table"],
        "parameters": [
          {"name": "schema_name", "in": "path", "description": "Schema name",
            "required": true, "type": "string"},
          {"name": "table_name", "in": "path", "description": "Table name",
            "required": true, "type": "string"}],
        "responses":  {"200" : {"description": "OK"}},
        "produces": ["application/json"]
      }
    },
    "/api/column/{schema_name}": {
      "get": {
        "tags": ["Column"],
        "parameters": [
          {"name": "schema_name", "in": "path", "description": "Schema name",
            "required": true, "type": "string"}],
        "responses":  {"200" : {"description": "OK"}},
        "produces": ["application/json"]
      }
    },
    "/api/column/{schema_name}.{table_name}": {
      "get": {
        "tags": ["Column"],
        "parameters": [
          {"name": "schema_name", "in": "path", "description": "Schema name",
            "required": true, "type": "string"},
          {"name": "table_name", "in": "path", "description": "Table name",
            "required": true, "type": "string"}],
        "responses":  {"200" : {"description": "OK"}},
        "produces": ["application/json"]
      }
    },
    "/api/column/{schema_name}.{table_name}.{column_name}": {
      "get": {
        "tags": ["Column"],
        "parameters": [
          {"name": "schema_name", "in": "path", "description": "Schema name",
            "required": true, "type": "string"},
          {"name": "table_name", "in": "path", "description": "Table name",
            "required": true, "type": "string"},
          {"name": "column_name", "in": "path", "description": "Column name",
            "required": true, "type": "string"}],
        "responses":  {"200" : {"description": "OK"}},
        "produces": ["application/json"]
      }
    }
  },
  "consumes": [
    "application/json"
  ]
}